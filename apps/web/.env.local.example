# Base URL of the justdoit REST API (apps/api). Defaults to http://localhost:8787 if unset.
NEXT_PUBLIC_API_URL=http://localhost:8787

# Optional: forwarded as the X-API-Key header on every request, if the API requires it.
# NEXT_PUBLIC_API_KEY=
